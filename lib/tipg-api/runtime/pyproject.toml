[project]
name = "tipg-api"
version = "0.0.0"
description = "tipg-api runtime"
requires-python = ">=3.12"
dependencies = [
    "mangum==0.19",
    "tipg>=1.2,<1.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
